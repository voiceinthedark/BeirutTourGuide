<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <TextView
            android:id="@+id/overview_weather"
            style="@style/overview_style"
            android:text="@string/weather"
            android:background="@drawable/weather_crop"/>

        <TextView
            android:id="@+id/overview_customs"
            style="@style/overview_style"
            android:text="@string/customs"
            android:background="@drawable/customs_crop"/>

        <TextView
            android:id="@+id/overview_food"
            style="@style/overview_style"
            android:text="@string/food"
            android:background="@drawable/mezza_crop"/>

        <TextView
            android:id="@+id/overview_alcohol"
            style="@style/overview_style"
            android:text="@string/alcohol"
            android:background="@drawable/arak_crop"/>

    </LinearLayout>
</ScrollView>